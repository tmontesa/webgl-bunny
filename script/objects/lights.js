function AmbientLight(ambient) {
    this.ambient = ambient;
}

function PointLight(position, diffuse, specular) {
    this.position = position;
    this.diffuse = diffuse;
    this.specular = specular;
}